machine:
  java:
    version: openjdk7

dependencies:
  pre:
    - wget -q 
    - curl -sSL https://dl.bintray.com/sbt/debian/sbt-0.13.5.deb -o sbt.deb
    - sudo dpkg -i sbt.deb
  cache_directories:
    - $HOME/.sbt
    - $HOME/.ivy2

test:
  override:
    - sbt test
